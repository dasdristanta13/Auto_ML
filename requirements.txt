langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.6
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
imbalanced-learn>=0.12
optuna>=3.6
joblib>=1.3
pyyaml>=6.0
python-dotenv>=1.0
jinja2>=3.1

# API backend + frontend hosting
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# LLM providers (install only the ones you plan to use)
anthropic>=0.34.0
openai>=1.40.0
google-generativeai>=0.7.0
litellm>=1.50.0   # only needed when config/models.yaml sets backend: litellm

# reporting / explainability
shap>=0.45.0
matplotlib>=3.8

# optional additional model libraries — when not installed, model selection
# automatically skips these candidate families (src/agents/model_selection_node.py)
# xgboost>=2.0
# lightgbm>=4.0

# testing
pytest>=8.0
